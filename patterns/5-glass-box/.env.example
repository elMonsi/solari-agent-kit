# Get a key at https://console.getsolari.com
# Only needed for a LIVE run (recording + replay download).
# `python main.py --offline` and the whole replay.py utility need no key.
SOLARI_API_KEY=slr_live_...
